# Виды оконных функций

### Агрегирующие
 - avg(col) over ()
 - count() over ()
 - max() over ()
 - min() over ()
 - sum() over ()
 

### Ранжирования
 - row_number()
 - rank()
 - dense_rank()


### Функции распределения
 - percent_rank()
 - cume_dist()

### Аналитические 
 - lead()
 - lag()
 - ntile()
 - first_value()
 - last_value()
 - nth_value()